fastapi==0.115.0
uvicorn[standard]==0.30.6
google-cloud-bigquery==3.25.0
google-auth==2.34.0
pydantic==2.9.2
python-dotenv==1.0.1
python-multipart==0.0.12
google-genai==0.3.0
# Anthropic SDK + httpx pin (Phase 9 fix). Without pinning these together,
# pip resolves to a newer httpx that breaks the older anthropic Client
# constructor with "unexpected keyword argument 'proxies'". Upgrading both
# to the versions below keeps them compatible.
anthropic>=0.40.0
httpx>=0.27.0
